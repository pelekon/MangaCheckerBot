using System;

namespace MangaChecker.Attributes
{
    public class AutoRegister : Attribute
    {
        public AutoRegister(object target, Type type)
        {
            //var methods = target.GetType().GetMethods();
        }
    }
}